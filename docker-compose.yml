version: "3"

services:
  ng_task:
    build:
      context: .
    ports:
      - "8000:8000"
    volumes:
      - ./ng_task:/ng_task
    command: >
      sh -c "python manage.py migrate &&
             python manage.py runserver 0.0.0.0:8000"
